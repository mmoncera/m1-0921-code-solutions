/* exported getLastChar */
function getLastChar(string) {
  return string[string.length - 1];
}

// define a function named getLastChar which takes a single parameter of 'string'
// return the parameter 'string' at the last index (result of subracting 1 from the length of 'string')
