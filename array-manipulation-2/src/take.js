/* exported take */
function take(array, count) {
  return array.slice(0, count);
}

// define a function named take which takes 2 paramaters of 'array' and 'count' respectively
// return the first # of characters (based on the value of the parameter 'count') of the 'array' paramater
