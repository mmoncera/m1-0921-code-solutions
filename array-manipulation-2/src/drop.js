/* exported drop */
function drop(array, count) {
  return array.slice(count);
}

// define a function named capitalizeWord which takes a sigle paramater of 'word'
// return the parameter 'array' starting from the index of the 'count' parameter
