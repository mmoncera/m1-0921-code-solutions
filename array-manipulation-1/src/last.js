/* exported last */
function last(array) {
  return array[array.length - 1];
}

// define a function named last which takes a sigle paramater of 'array'
// return the paramater 'array' at the last index (result of subracting 1 from the length of 'array')
