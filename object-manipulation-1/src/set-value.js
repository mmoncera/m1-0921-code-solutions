/* exported setValue */
function setValue(object, key, value) {
  object[key] = value;
}

// define a function named setValue which takes 3 paramaters of 'object', 'key', and 'value' respectively
// assign the parameter 'value' to the parameters 'object' at 'key'
