/* exported getValue */
function getValue(object, key) {
  return object[key];
}

// define a function named getValue which takes two paramaters of 'object' and 'key' respectively
// return the value from the parameters 'object' at 'key'
